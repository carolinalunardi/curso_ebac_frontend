function multiplicar(n1: number, n2: number): number {
    return n1 * n2;
}

function saudacao(nome: string): string {
    return `Olá ${nome}`;
}

console.log(multiplicar(2, 10))
console.log(saudacao("Carolina"))